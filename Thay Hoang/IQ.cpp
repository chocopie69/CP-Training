#include <bits/stdc++.h>
#define ll long long
using namespace std;

const int MAXN = 1e5+2;
ll a[MAXN],b[MAXN];

int main()
{
    cin >> n;
    for(int i=0;i<n;i++) cin >> a[i];
    for(int i=0;i<n;i++) cin >> b[i];
    sort(a,a+n,greater<int>());
    sort(b,b+n);

}
